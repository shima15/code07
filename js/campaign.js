function campaignNumber(user){
    if(user.age >= 30 &&
       user.sex == 1 &&
       user.married == true){
           return 1;
           
       }
 
 
    var user = {
        age: age,
        sex: sex,
        married: married,
        campaignNumber: campaignNumber
    }
        return user;